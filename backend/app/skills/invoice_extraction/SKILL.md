# Invoice Extraction

skill_id: invoice_extraction
name: Invoice Extraction
description: Extract invoice numbers, vendor names, totals, due dates, and line item hints.
supported_document_types: pdf, docx, image

